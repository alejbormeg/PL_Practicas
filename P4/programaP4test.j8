main ()
{
	var {
		int v b,n,k
		int z12 z343;
		int ve2;
		float vf,s,d, f ;
		char vc ;
		bool v1 ;
		list_of int pe, pe2 ;
		list_of  float pf, pf2 ;
		list_of bool p1;
		list_of char pd;
		list_of    int ppf;
	}

 int funcionA (int a1, float a2, char a3) {
 		var {
			int x1, x2 ;
		}

		char funcionB(char b1, bool b2) {
			var {
				float xf, x2 ;
			}

			float funcionC (bool c1, int c2) {
				var {
					float x1;
				}

				x1 = 1.3;
				v=ve2*2;
				v=funcionA(2*5,2.5,'a');
				if ((c2>10))
					c2= c2-1 ;
				else
					x1= 3.1 ;
				return x1;
			}

			xf = funcionC (true, 10);
			x2 = xf * (funcionC(false, 1)-funcionC(true,23)) /10.0;

			while (x2*funcionC(false,1)-xf<10.0)
				x2 = x2*xf;
			}

		int funcionF() {

		}

		float funcionD (float d1) 			{
			var {
				char dato;
				int valor ;
			}

			char funcionE (char e1, char e2) {
				output "introduzca ", (3-1), " caracteres: " ;
				if (e1 =='a')
					return e1 ;
				else
					return ' ';
			}

			output "introduzca un valor entero" ;

			if (d1>0.0) {
				var {
					int dato ;
				}

				dato = 2;
				dato = valor*20/dato;
			} else {
				valor = valor * 100 ;
				d1 = d1/1000.0 ;
			}
			return d1 ;
		}
 	}

	pe = [1, 43, 5, 0, 0];
	pd = ['z', 'A', 'B'];
	pf = [2.5, 4.9, 3.0, 0.0];
	p1 = [true, false, true,true] ;
	pe >> ;
	pe << ;
	pe << ;
	$pe ;
	if (#pe < ?pe2)
		pe@2;
	else
		pe @ 3 ;
	pe = pe**(ppf % 2) ;
	p1 = p1 ++true@3 ;
	pf = pf -- (3 + 2 / 2) ;
	pe = pe ;
	pe= pe*2;
	pe = pe ++ 3 @ 2;

	pe = pe / 3;
}
